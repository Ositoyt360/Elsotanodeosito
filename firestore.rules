rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Chat publico: cualquiera puede leer y publicar mensajes.
    match /livechat/{messageId} {
      allow read, create: if true;
      allow update, delete: if false;
    }
  }
}
